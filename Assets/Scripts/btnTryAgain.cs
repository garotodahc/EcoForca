using UnityEngine;
using System.Collections;

public class btnTryAgain : MonoBehaviour {


 //   public AudioSource audio;
    // Use this for initialization
    void Start () {
	
	}

    // Update is called once per frame
    public void ChangeToscene()
    {
        //Seto variável static para puder verificar se user clicou em repetir ou prosseguir level.
        //StatusScript.levelToChange = sceneToChange;

      //  audio.Play();
       // WaitForSeconds wait = new WaitForSeconds(audio.clip.length + 1.0f);

        //Application.LoadLevel(sceneToChange);
        //   SceneManager.LoadScene(sceneToChange);

   //     for (int i = 0; i <=GerenciadorPalavras.minhalista.Palavras.Count; i++)
                {
            //recarrega a cena com uma nova palavra
 //           GerenciadorPalavras.chamaTela = false;
 //           Application.LoadLevel(Application.loadedLevel);
                        



                  }
    }
}
